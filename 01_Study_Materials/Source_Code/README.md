# Deployment source required

The deployed HTML/JavaScript/JSON was not present in the response spreadsheet or local project snapshot used to assemble this package. Copy the exact frozen deployment source into this directory before the OSF project is made public. Do not replace it with a reconstructed implementation unless that fact is clearly labeled.

